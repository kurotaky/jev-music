[project]
name = "jev-music"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yuta Kurotaki", email = "yuta.kurotaki@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "librosa>=1.0.0",
    "numpy>=2.5.3",
    "soundfile>=0.14.0",
]

[project.scripts]
jev-music = "jev_music:main"

[build-system]
requires = ["uv_build>=0.11.24,<0.12.0"]
build-backend = "uv_build"
